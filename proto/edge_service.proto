syntax = "proto3";

package iot.v1;

import "common.proto";
import "google/protobuf/timestamp.proto";

service EdgeService {
  rpc StreamTelemetry(stream SensorData) returns (Ack);
}

message Ack {
  bool success = 1;
  string message = 2;
}
